{
  "name": "maintenance-memo-stlite",
  "version": "1.0.0",
  "dependencies": {
    "@stlite/mountable": "latest"
  },
  "scripts": {
    "build": "npx -p @stlite/mountable stlite-build frontend/app.py --output dist"
  }
}
